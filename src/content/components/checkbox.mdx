---
title: Checkbox
description: Checkbox allows users to make a binary choice—either toggling a single option on or off or selecting multiple options from a list.
features:
  - "Can be controlled or uncontrolled"
  - "Multiple variants, sizes and border radiuses"
  - "Supports indeterminate state"
  - "Handles invalid appearance"
  - "Supports custom icon and styles"
  # - "Respects reduce motion with system settings and through props"
---

<ComponentShowcase name="checkbox-demo" />

## Installation

<Tabs defaultValue="terminal">
  <TabsList>
    <TabsTrigger value="terminal">
      <Terminal className="w-4 h-4" />
      Terminal
    </TabsTrigger>
    <TabsTrigger value="manual">
      <Code2 className="w-4 h-4" />
      Manual
    </TabsTrigger>
  </TabsList>
  <TabsContent value="terminal">
    <InstallationCommands
      type="registry-installation"
      registryPath="components/checkbox"
    />
  </TabsContent>
  <TabsContent value="manual">
    <Steps>
      <Step>Install dependencies</Step>

      <InstallationCommands
        type="package-installation"
        npmCommand="npm install class-variance-authority clsx tailwind-merge @base-ui-components/react"
        pnpmCommand="pnpm add class-variance-authority clsx tailwind-merge @base-ui-components/react"
        yarnCommand="yarn add class-variance-authority clsx tailwind-merge @base-ui-components/react"
        bunCommand="bun add class-variance-authority clsx tailwind-merge @base-ui-components/react"
      />

      <Step>Add cn function to your utils</Step>

      ```ts filename="lib/classes.ts"
      import { clsx, type ClassValue } from "clsx";
      import { twMerge } from "tailwind-merge";

      export function cn(...inputs: ClassValue[]) {
        return twMerge(clsx(inputs));
      }
      ```

      <Step>Copy and paste the following code into your project</Step>

      <ComponentCodePreview name="checkbox" />
    </Steps>

  </TabsContent>
</Tabs>

## Anatomy

```tsx
import { Checkbox, CheckboxRoot, CheckboxIndicator } from "@/components/ui/checkbox";

// Option #1. Basic usage:
<Checkbox />

// Option #2. Custom usage:
<CheckboxRoot>
  <CheckboxIndicator />
</CheckboxRoot>
```

## Usage

### Disabled

<ComponentShowcase name="checkbox-disabled-demo" />

### Sizes

<ComponentShowcase name="checkbox-sizes-demo" />

### Radius

<ComponentShowcase name="checkbox-radius-demo" />

### Custom Icons

<ComponentShowcase name="checkbox-icons-demo" />
