---
title: Input
description: Input is a component that allows users to enter text. It can be used to get user inputs in forms, search fields, and more.
---

<ComponentShowcase name="input-demo" />

## Installation

<Tabs defaultValue="terminal">
  <TabsList>
    <TabsTrigger value="terminal">
      <Terminal className="w-4 h-4" />
      Terminal
    </TabsTrigger>
    <TabsTrigger value="manual">
      <Code2 className="w-4 h-4" />
      Manual
    </TabsTrigger>
  </TabsList>
  <TabsContent value="terminal">
    <InstallationCommands
      type="registry-installation"
      registryPath="components/input"
    />
  </TabsContent>
  <TabsContent value="manual">
    <Steps currentStep={1}>
      <Step stepNumber={1} title="Install dependencies">
        <InstallationCommands
          type="package-installation"
          npmCommand="npm install tailwind-variants clsx tailwind-merge"
          pnpmCommand="pnpm add tailwind-variants clsx tailwind-merge"
          yarnCommand="yarn add tailwind-variants clsx tailwind-merge"
          bunCommand="bun add tailwind-variants clsx tailwind-merge"
        />
      </Step>
      <Step
        stepNumber={2}
        title="Add cn function to your utils"
      >
        ```ts
        import { clsx, type ClassValue } from "clsx";
        import { twMerge } from "tailwind-merge";

        export function cn(...inputs: ClassValue[]) {
          return twMerge(clsx(inputs));
        }
        ```
      </Step>
      <Step
        stepNumber={3}
        title="Copy and paste the following code into your project"
        isLast
      >
        <ComponentCodePreview name="input" />
      </Step>
    </Steps>

  </TabsContent>
</Tabs>

## Usage

### Disabled

<ComponentShowcase name="input-disabled-demo" />

### Read Only

<ComponentShowcase name="input-read-only-demo" />

### Required

If you pass the `required` property to the input, it will have a `danger` asterisk at the end of the label and the input will be required.

<ComponentShowcase name="input-required-demo" />

### Sizes

<ComponentShowcase name="input-sizes-demo" />

### Variants

<ComponentShowcase name="input-variants-demo" />
