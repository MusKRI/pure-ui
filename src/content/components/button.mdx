---
title: Button
description: Used to trigger an action or event, such as submitting a form, displaying a dialog or sending a request.
features:
  - "A wrapper around native HTML <button>, supports all HTMLButtonElement properties, methods and events"
  - "Different variants, sizes and border radiuses"
  - "Pending state animation"
  - "Respects reduce motion settings"
---

<ComponentShowcase name="button-demo" />

## Installation

<Tabs defaultValue="terminal">
  <TabsList>
    <TabsTrigger value="terminal">
      <Terminal className="w-4 h-4" />
      Terminal
    </TabsTrigger>
    <TabsTrigger value="manual">
      <Code2 className="w-4 h-4" />
      Manual
    </TabsTrigger>
  </TabsList>
  <TabsContent value="terminal">
    <InstallationCommands
      type="registry-installation"
      registryPath="components/button"
    />
  </TabsContent>
  <TabsContent value="manual">
    <Steps currentStep={1}>
      <Step stepNumber={1} title="Install dependencies">
        <InstallationCommands
          type="package-installation"
          npmCommand="npm install tailwind-variants clsx tailwind-merge @base-ui-components/react"
          pnpmCommand="pnpm add tailwind-variants clsx tailwind-merge @base-ui-components/react"
          yarnCommand="yarn add tailwind-variants clsx tailwind-merge @base-ui-components/react"
          bunCommand="bun add tailwind-variants clsx tailwind-merge @base-ui-components/react"
        />
      </Step>
      <Step
        stepNumber={2}
        title="Add cn function to your utils"
      >
        ```ts
        import { clsx, type ClassValue } from "clsx";
        import { twMerge } from "tailwind-merge";

        export function cn(...inputs: ClassValue[]) {
          return twMerge(clsx(inputs));
        }
        ```
      </Step>
      <Step
        stepNumber={3}
        title="Copy and paste the following code into your project"
        isLast
      >
        <ComponentCodePreview name="button" />
      </Step>
    </Steps>

  </TabsContent>
</Tabs>

## Usage

```tsx
import { Button } from "@/components/ui/button";
```

```tsx
<Button>Button</Button>
```

## Link

You can use the `render` prop to make another component look like a button. Here's an example of a link that looks like a button.

```tsx
import Link from "next/link";

import { Button } from "@/components/ui/button";

export function LinkAsButton() {
  return <Button render={<Link href="/login" />}>Login</Button>;
}
```

## Examples

### Variants

<ComponentShowcase name="button-variants-demo" />

### Size

<ComponentShowcase name="button-size-demo" />

### Radius

<ComponentShowcase name="button-radius-demo" />

### Pending

<ComponentShowcase name="button-pending-demo" />

### Reduce Motion

<ComponentShowcase name="button-reduce-motion-demo" />

## Comparing with Radix / Shadcn

If you’re already familiar with Radix UI and shadcn/ui, this guide highlights the small differences and similarities so you can get started with **Pure UI** quickly.

### Quick Checklist

- Replace `asChild` → `render` on `Button`

### Comparison Example

```tsx remove=2,5 add=3
<Button
  asChild
  render={<Link href="/login" />}
>
  <Link href="/login">Log in</Link>
  Log in
</Button>
```
